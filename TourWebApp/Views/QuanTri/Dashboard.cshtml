@{
    Layout = "_LayoutAdmin";
}

<link rel="stylesheet" href="~/css/dashboard-admin.css" />

<h3 class="mb-4 fw-bold">
    📊 Tổng quan hệ thống HappyTrip
</h3>

<!-- LOADING -->
<div id="loadingScreen">
    <div class="spinner"></div>
    <span>Đang tải dữ liệu...</span>
</div>

<div class="row g-4 mb-3">
    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-blue text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-black-50">Tổng tour</small>
                    <h2 class="fw-bold mb-0 counter" data-count="@ViewBag.TongTour">0</h2>
                </div>
                <i class="fa fa-map-location-dot fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-green text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Tour đang bán</small>
                    <h2 class="fw-bold mb-0 counter" data-count="@ViewBag.TourDangBan">0</h2>
                </div>
                <i class="fa fa-check-circle fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-gray text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Tour ngưng bán</small>
                    <h2 class="fw-bold mb-0 counter" data-count="@ViewBag.TourNgungBan">0</h2>
                </div>
                <i class="fa fa-pause-circle fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-red text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Đơn đặt tour</small>
                    <h2 class="fw-bold mb-0 counter" data-count="@ViewBag.TongDon">0</h2>
                </div>
                <i class="fa fa-receipt fa-2x"></i>
            </div>
        </div>
    </div>
</div>

<div class="row g-4 mb-4">
    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-purple text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Tổng doanh thu</small>
                    <h4 class="fw-bold mb-0">@string.Format("{0:N0} đ", ViewBag.TongDoanhThu ?? 0)</h4>
                </div>
                <i class="fa fa-sack-dollar fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-orange text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Tổng khách đã đặt</small>
                    <h2 class="fw-bold mb-0">@(ViewBag.TongKhach ?? 0)</h2>
                </div>
                <i class="fa fa-user-group fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-gray text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Tổng người dùng</small>
                    <h2 class="fw-bold mb-0">@ViewBag.TongNguoiDung</h2>
                </div>
                <i class="fa fa-users fa-2x"></i>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
        <div class="card stat-card bg-green text-white shadow-sm h-100">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <small class="text-white-50">Đơn đã thanh toán</small>
                    <h2 class="fw-bold mb-0">@ViewBag.SoDonDaThanhToan</h2>
                </div>
                <i class="fa fa-circle-check fa-2x"></i>
            </div>
        </div>
    </div>
</div>


<div class="row mb-4">
    <div class="col-12">
        <div class="card shadow-sm p-3 chart-card">
            <h6 class="fw-bold mb-3">💰 Doanh thu 14 ngày gần nhất</h6>
            <div class="chart-wrapper chart-big">
                <canvas id="chartDoanhThuNgay"></canvas>
            </div>
        </div>
    </div>
</div>

<div class="row mb-4">
    <div class="col-md-12">
        <div class="card shadow-sm p-3 chart-card">
            <h6 class="fw-bold mb-3">📈 Tình trạng tour</h6>

            <div class="chart-wrapper">
                <canvas id="chartTrangThai"
                        data-dangban="@ViewBag.TourDangBan"
                        data-ngungban="@ViewBag.TourNgungBan"></canvas>
            </div>
        </div>
    </div>
</div>
<div class="row mb-4">
    <div class="col-md-6">
        <div class="card shadow-sm p-3 chart-card">
            <h6 class="fw-bold mb-3">📊 Đơn đặt tour theo trạng thái thanh toán</h6>
            <div class="chart-wrapper">
                <canvas id="chartDonTrangThai"></canvas>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card shadow-sm p-3 chart-card">
            <h6 class="fw-bold mb-3">📈 Xu hướng đặt tour theo ngày</h6>
            <div class="chart-wrapper">
                <canvas id="chartDonTheoNgay"></canvas>
            </div>
        </div>
    </div>
</div>

<hr class="my-4" />

<h5 class="fw-bold mb-3">🔥 Top 3 tour được đặt nhiều nhất</h5>

<table class="table table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>Tour</th>
            <th>Địa điểm</th>
            <th>Số người đặt</th>
            <th>Mức độ hot</th>
        </tr>
    </thead>

@{
    var topTours = ViewBag.TopTour as IEnumerable<dynamic> ?? Enumerable.Empty<dynamic>();
    int max = topTours.Any() ? topTours.Max(t => (int)t.SoNguoiDaDat) : 0;
}

<tbody>
@foreach (var t in topTours)
{
    int percent = max == 0 ? 0 : (int)((double)t.SoNguoiDaDat / max * 100);

    <tr>
        <td>@t.TenTour</td>
        <td>@t.DiaDiem</td>
        <td>
            <span class="badge bg-primary">@t.SoNguoiDaDat người</span>
        </td>
        <td style="width:30%">
            <div class="progress" style="height:8px;">
                <div class="progress-bar bg-success" style="width:@percent%"></div>
            </div>
        </td>
    </tr>
}
</tbody>
</table>
<script>
    window.soDonDaThanhToan = @ViewBag.SoDonDaThanhToan;
    window.soDonChuaThanhToan = @ViewBag.SoDonChuaThanhToan;
    window.soDonHetHan = @ViewBag.SoDonHetHan;

    window.labelDoanhThuNgay = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.LabelDoanhThuNgay ?? new List<string>()));
    window.dataDoanhThuNgay  = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.DataDoanhThuNgay ?? new List<decimal>()));

    window.labelNgay = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.LabelNgay ?? new List<string>()));
    window.dataNgay  = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.DataNgay ?? new List<int>()));
</script>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="~/js/dashboard-admin.js"></script>
